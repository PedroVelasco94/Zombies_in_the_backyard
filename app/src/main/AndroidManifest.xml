<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pedrovelasco.zombiesinthebackyard">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".principal" />
        <activity android:name=".zombiesLanzar" />
        <activity android:name=".BarricadasLanzar" />
        <activity android:name=".RepararLanzar" />
        <activity android:name=".GameOver" />
        <activity android:name=".Victory" />
        <activity android:name=".Howto"></activity>
        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    </application>

</manifest>
